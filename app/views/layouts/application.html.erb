<!DOCTYPE html>
<html lang="en">
<head>
  <title>RubyBlog</title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%= stylesheet_link_tag 'application', media: 'all' %>
  <%= javascript_include_tag 'application' %>
</head>

<body>
<nav class="navbar navbar-light bg-light">
  <h1 class="navbar-brand">RubyBlog</h1>
  <% if user_signed_in? %>
    <div class="nav-section"> <%= link_to 'Nouveau post', [:new, :post], class: 'btn btn-success' %></div>
  <% end %>

  <div class="nav-item">
    <% if user_signed_in? %>
      <p>Connected as: <%= current_user.first_name %> <%= current_user.last_name %></p>
      <%= link_to('Logout', destroy_user_session_path, method: :delete, :class => "badge badge-secondary") %>
    <% else %>
      <%= link_to('Login', new_user_session_path, method: :post, :class => "badge badge-primary") %>
      <%= link_to('Register', new_user_registration_path, :class => "badge badge-secondary") %>
    <% end %>
  </div>
</nav>

<div class="container">
  <%= yield %>
</div>
</body>
</html>
