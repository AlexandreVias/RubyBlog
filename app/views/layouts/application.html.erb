<!DOCTYPE html>
<html lang="en">
<head>
  <title>RubyBlog</title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%= stylesheet_link_tag 'application', media: 'all' %>
  <%= javascript_include_tag 'application' %>
</head>

<body>
<h1>RubyBlog</h1>

<% if user_signed_in? %>
  <p>Connected as: <%= current_user.first_name %> <%= current_user.last_name %></p>
  <%= link_to('Logout', destroy_user_session_path, method: :delete) %>
<% else %>
  <%= link_to('Login', new_user_session_path, method: :post) %>
<% end %>

<%= yield %>
</body>
</html>
